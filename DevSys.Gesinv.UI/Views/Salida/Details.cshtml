@model IEnumerable<DevSys.Gesinv.UI.Models.ViewModels.SalidaViewModel>

@{
  ViewData["Title"] = "Salida - Detalles";
}

<!DOCTYPE html>

<html>
<head>
  <meta name="viewport" content="width=device-width"/>
  <title>Index</title>
</head>
<body>
  <h1>Detalle de Salida</h1>

  <h3 align="center">SALIDA</h3>
@*  <table class="table">
    <thread>
      <tr align="center">
        <th>
          @Html.DisplayNameFor(model => model.LineaSalidaId)
        </th>
        <th>
          @Html.DisplayNameFor(model => model.MotivoNombre)
        </th>
        <th>
          @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
          @Html.DisplayNameFor(model => model.RequisicionCodigo)
        </th>
        <th>
          @Html.DisplayNameFor(model => model.BodegaNombre)
        </th>
        <th></th>
      </tr>
    </thread>
    <tbody>
      @foreach (var item in Model)
      {
          <tr align="center">
            <td>
            @Html.DisplayFor(modelItem => item.LineaSalidaId)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.MotivoNombre)
            </td>
              <td type="date(dd-mm-yyyy)">
            @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.RequisicionCodigo)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.BodegaNombre)
            </td>
            <td>
            @Html.ActionLink("Detalles", "Details", new { id=item.SalidaId }) |
            @Html.ActionLink("Editar", "Edit", new { id=item.SalidaId }) |
            @Html.ActionLink("Eliminar", "Eliminar", new { id=item.SalidaId }) |
            <a asp-action="Details" asp-route-id="@item.SalidaId">Detalles</a>
            <a asp-action="Edit" asp-route-id="@item.SalidaId">Editar</a>
            <a asp-action="Delete" asp-route-id="@item.SalidaId">Eliminar</a>
            </td>
          </tr>
      }
    </tbody>
  </table>*@
</body>
</html>